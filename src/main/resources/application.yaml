spring:
  datasource:
    url: jdbc:mysql://localhost:3306/checkin?allowMultiQueries=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    io.yudoge.checkin.repositories: DEBUG
